# 英语学习系统 V6 实现原理详解

> 本手册面向零基础读者，逐行解释代码含义，读完可手写复现整个项目。

---

## 一、项目概览

### 1.1 这是什么？

一个**英语学习 Web 系统**，功能：
- 学习卡片：展示英文 → 点击翻转看中文
- 测试模式：看中文 → 敲英文单词逐词检测
- 复习模式：只显示需要复习的短语
- 后台管理：增删改短语、分类、上传/录制音频
- 录音功能：浏览器麦克风录制 → 自动转 MP3 → 关联到短语

### 1.2 技术栈

| 层次 | 技术 | 作用 |
|------|------|------|
| 后端框架 | FastAPI (Python) | 提供 API 接口 |
| 数据库 | SQLite | 存储短语、进度 |
| 前端 | 原生 HTML + CSS + JS | 浏览器页面 |
| 音频转换 | pydub + ffmpeg | 录音转 MP3 |
| 服务器 | Uvicorn | 运行 FastAPI |

### 1.3 目录结构

```
v6/
├── app/                    # Python 后端
│   ├── main.py             # 入口文件
│   ├── database.py         # 数据库操作
│   ├── models.py           # 数据模型定义
│   └── routers/            # API 路由
│       ├── categories.py   # 分类接口
│       ├── phrases.py      # 短语接口
│       ├── progress.py     # 学习进度接口
│       ├── stats.py        # 统计接口
│       └── audio.py        # 音频接口
├── public/                 # 前端静态文件
│   ├── index.html          # 学习页面
│   ├── admin.html          # 后台管理页面
│   └── src/
│       ├── app.js          # 学习页面逻辑
│       ├── admin.js        # 后台管理逻辑
│       ├── styles.css      # 学习页面样式
│       └── admin.css       # 后台管理样式
├── data/
│   ├── english.db          # SQLite 数据库文件（自动生成）
│   └── phrases.json        # 初始种子数据
├── requirements.txt        # Python 依赖
├── start.sh                # Linux 启动
└── start.bat               # Windows 启动
```

### 1.4 启动方式

```bash
# Linux/Mac
pip install -r requirements.txt
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
# 浏览器打开 http://localhost:8000

# Windows
双击 start.bat
```

---

## 二、请求响应流程

### 2.1 用户访问页面时发生了什么？

```
浏览器 → http://localhost:8000/
                   ↓
FastAPI 收到请求 → StaticFiles 查找 public/index.html
                   ↓
返回 index.html → 浏览器解析 HTML
                   ↓
发现 <link href="src/styles.css"> → 再请求 /src/styles.css
发现 <script src="src/app.js">    → 再请求 /src/app.js
                   ↓
DOMContentLoaded 事件触发 → 调用 init()
                   ↓
init() 调用 API.getCategories() → fetch('/api/categories')
                   ↓
FastAPI 路由匹配 → categories.list_categories() → 查 SQLite
                   ↓
返回 JSON → JS 渲染分类按钮列表
                   ↓
调用 API.getPhrases() → fetch('/api/phrases')
                   ↓
返回短语列表 → 更新卡片 UI
```

### 2.2 API 路由总表

| 方法 | 路径 | 功能 | 所在文件 |
|------|------|------|----------|
| GET | /api/categories | 获取所有分类 | categories.py |
| POST | /api/categories | 新建分类 | categories.py |
| PUT | /api/categories/{id} | 修改分类 | categories.py |
| DELETE | /api/categories/{id} | 删除分类 | categories.py |
| GET | /api/phrases | 获取短语列表 | phrases.py |
| POST | /api/phrases | 新建短语 | phrases.py |
| PUT | /api/phrases/{id} | 修改短语 | phrases.py |
| DELETE | /api/phrases/{id} | 删除短语 | phrases.py |
| POST | /api/progress/{id} | 提交答题结果 | progress.py |
| DELETE | /api/reset | 重置进度 | progress.py |
| GET | /api/stats | 获取统计数据 | stats.py |
| GET | /api/review | 获取需复习短语 | phrases.py |
| POST | /api/upload-audio | 上传音频文件 | phrases.py |
| GET | /api/audio-files | 列出音频文件 | audio.py |
| DELETE | /api/audio/{name} | 删除音频文件 | audio.py |
| POST | /api/record-audio/{id} | 录音并转 MP3 | audio.py |

---

## 三、后端代码详解

### 3.1 `app/main.py` — 系统入口

```python
from contextlib import asynccontextmanager
from pathlib import Path
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
```

**`contextlib.asynccontextmanager`**：Python 标准库，用于创建"异步上下文管理器"。代码中用 `@asynccontextmanager` 装饰器修饰 `lifespan` 函数，让 FastAPI 在**启动时**和**关闭时**执行特定代码。

**`pathlib.Path`**：Python 标准库，用于跨平台路径操作。用 `/` 拼接路径（比 `os.path.join` 更直观）。

**`fastapi`**：高性能 Web 框架。关键概念：
- `FastAPI`：创建应用实例
- `CORSMiddleware`：跨域中间件，允许浏览器跨域请求
- `StaticFiles`：挂载静态文件目录，让浏览器能直接访问 HTML/CSS/JS

```python
from app.database import init_db, seed_from_json
from app.routers import categories, phrases, progress, stats, audio
```

**`from app.database import ...`**：从 app/database.py 导入两个函数。Python 用 `import` 导入其他文件的代码，`.` 表示"当前包"。

**`from app.routers import ...`**：从 app/routers/ 目录导入 5 个路由模块（Python 会把目录当"包"是因为里面有 `__init__.py`）。

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    init_db()
    seed_from_json()
    yield
```

**`lifespan` 函数**：FastAPI 的生命周期钩子。
- `@asynccontextmanager`：将这个函数变成上下文管理器
- `async def`：异步函数定义
- `app: FastAPI`：函数参数，类型注解为 FastAPI 实例
- `init_db()`：启动时调用，创建数据库表（如果不存在）
- `seed_from_json()`：启动时调用，从 JSON 导入初始数据
- `yield`：暂停函数，FastAPI 开始处理请求。当服务关闭时，yield 后面的代码会执行（这里没有后续代码）

```python
app = FastAPI(title="英语学习系统 V4", lifespan=lifespan)
```

创建 FastAPI 实例：
- `title`：API 文档标题（访问 /docs 可看）
- `lifespan`：指定生命周期函数

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**CORS 跨域配置**：
- `allow_origins=["*"]`：允许所有域名访问（开发方便，生产环境应限制）
- `allow_methods=["*"]`：允许所有 HTTP 方法（GET/POST/PUT/DELETE等）
- `allow_headers=["*"]`：允许所有请求头

```python
app.include_router(categories.router, prefix="/api")
app.include_router(phrases.router, prefix="/api")
app.include_router(progress.router, prefix="/api")
app.include_router(stats.router, prefix="/api")
app.include_router(audio.router, prefix="/api")
```

**`include_router`**：注册路由模块。
- `categories.router`：从 categories.py 导入的 router 对象
- `prefix="/api"`：所有路由前加 `/api` 前缀
- 例如 categories.py 中的 `@router.get("/categories")` 实际变成 `GET /api/categories`

```python
public_path = Path(__file__).resolve().parent.parent / "public"
if public_path.exists():
    app.mount("/", StaticFiles(directory=str(public_path), html=True), name="public")
```

**静态文件挂载**：
- `Path(__file__)`：当前文件（main.py）的路径
- `.resolve()`：解析为绝对路径
- `.parent.parent`：上两级目录（从 app/ 到项目根目录）
- `/ "public"`：拼接 public 目录
- `app.mount("/", ...)`：将 public 目录挂载到根路径
- `StaticFiles(directory=..., html=True)`：静态文件服务，`html=True` 表示找不到文件时返回 index.html（支持前端路由）
- `if public_path.exists()`：仅在 public 目录存在时挂载

---

### 3.2 `app/database.py` — 数据库层

```python
import sqlite3
import json
import os
from pathlib import Path
```

**`sqlite3`**：Python 内置的 SQLite 数据库驱动，无需额外安装。SQLite 是一个**文件型数据库**，数据存在单个文件（.db）中。

**`json`**：Python 内置的 JSON 处理库，用于读写 JSON 文件。

```python
DATA_DIR = Path(__file__).resolve().parent.parent / "data"
DB_PATH = DATA_DIR / "english.db"
PHRASES_JSON = DATA_DIR / "phrases.json"

DATA_DIR.mkdir(parents=True, exist_ok=True)
```

**路径定义**：
- `DATA_DIR`：data/ 目录的绝对路径
- `DB_PATH`：数据库文件路径
- `PHRASES_JSON`：种子数据文件路径
- `DATA_DIR.mkdir(parents=True, exist_ok=True)`：如果 data/ 目录不存在则创建它。`parents=True` 同时创建父目录，`exist_ok=True` 目录已存在时不报错

---

#### `get_connection()` — 获取数据库连接

```python
def get_connection() -> sqlite3.Connection:
    conn = sqlite3.connect(str(DB_PATH), check_same_thread=False)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA foreign_keys=ON")
    return conn
```

**逐行分析**：
- `def get_connection() -> sqlite3.Connection`：定义函数，返回值类型为 Connection 对象
- `sqlite3.connect(str(DB_PATH), ...)`：连接到 SQLite 数据库文件。如果文件不存在会自动创建
- `check_same_thread=False`：允许在不同线程中使用同一个连接。FastAPI 在处理多个请求时会用不同线程
- `conn.row_factory = sqlite3.Row`：**关键设置！** 默认 sqlite3 返回元组（只能通过索引访问），改成 Row 后可以用列名访问：`row["name"]` 或 `row["id"]`
- `PRAGMA journal_mode=WAL`：设置数据库为 WAL（Write-Ahead Log）模式，**提升并发读写性能**
- `PRAGMA foreign_keys=ON`：启用外键约束，保证数据完整性。比如删除分类时自动删除该分类下的所有短语
- `return conn`：返回连接对象

---

#### `init_db()` — 初始化数据库表

```python
def init_db():
    conn = get_connection()
```

获取数据库连接，准备执行 SQL。

```python
    conn.executescript("""
        CREATE TABLE IF NOT EXISTS categories (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            name TEXT NOT NULL UNIQUE,
            description TEXT
        );
        ...
    """)
```

**`executescript()`**：一次执行多条 SQL 语句（用分号分隔）。

**SQL 语句详解**：

```sql
CREATE TABLE IF NOT EXISTS categories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    description TEXT
);
```

- `IF NOT EXISTS`：如果表已存在，跳过不报错
- `categories`：分类表
- `id INTEGER PRIMARY KEY AUTOINCREMENT`：自增主键，每插入一行自动 +1
- `name TEXT NOT NULL UNIQUE`：分类名称，不能为空，且必须唯一
- `description TEXT`：描述（可选）

```sql
CREATE TABLE IF NOT EXISTS phrases (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    category_id INTEGER NOT NULL,
    en TEXT NOT NULL,
    zh TEXT,
    phonetic TEXT,
    example TEXT,
    FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE
);
```

- `phrases`：短语表
- `category_id INTEGER NOT NULL`：所属分类 ID，不能为空
- `en TEXT NOT NULL`：英文，不能为空
- `zh TEXT`：中文（可选）
- `FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE`：**外键约束**。category_id 引用 categories 表的 id 列。`ON DELETE CASCADE` 表示如果分类被删除，该分类下的所有短语也自动删除

```sql
CREATE TABLE IF NOT EXISTS progress (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    phrase_id INTEGER NOT NULL UNIQUE,
    status TEXT DEFAULT 'new',
    correct_count INTEGER DEFAULT 0,
    wrong_count INTEGER DEFAULT 0,
    last_review INTEGER,
    next_review INTEGER,
    FOREIGN KEY (phrase_id) REFERENCES phrases(id) ON DELETE CASCADE
);
```

- `progress`：学习进度表
- `phrase_id INTEGER NOT NULL UNIQUE`：短语 ID，**唯一**（一个短语只有一条进度记录）
- `status TEXT DEFAULT 'new'`：学习状态，默认 "new"。取值：new（未学习）、learning（学习中）、mastered（已掌握）
- `correct_count INTEGER DEFAULT 0`：连续正确次数
- `wrong_count INTEGER DEFAULT 0`：连续错误次数
- `last_review INTEGER`：最后复习时间（毫秒时间戳）
- `next_review INTEGER`：下次复习时间（毫秒时间戳）

```sql
CREATE TABLE IF NOT EXISTS stats (
    id INTEGER PRIMARY KEY DEFAULT 1,
    total_answers INTEGER DEFAULT 0,
    correct_answers INTEGER DEFAULT 0,
    wrong_answers INTEGER DEFAULT 0
);

INSERT OR IGNORE INTO stats (id, total_answers, correct_answers, wrong_answers)
VALUES (1, 0, 0, 0);
```

- `stats`：统计表，只有一行数据（id=1）
- `INSERT OR IGNORE`：如果 id=1 的记录不存在则插入，已存在则忽略
- 初始化：所有计数为 0

```python
    for col in ("type", "audio_file"):
        try:
            conn.execute(f"ALTER TABLE phrases ADD COLUMN {col} TEXT")
        except sqlite3.OperationalError:
            pass
```

**后加字段**：因为早期版本表设计没有 `type` 和 `audio_file`，这里用 ALTER TABLE 添加（如果列已存在会报错，用 try/except 忽略）。这是**兼容旧数据库**的常用技巧。

---

#### `seed_from_json()` — 导入种子数据

```python
def seed_from_json(force: bool = False):
    if not PHRASES_JSON.exists():
        return
```

- `force: bool = False`：函数参数，默认不强制导入
- `if not PHRASES_JSON.exists()`：如果 JSON 文件不存在，直接返回

```python
    conn = get_connection()
    count = conn.execute("SELECT COUNT(*) FROM categories").fetchone()[0]
    if count > 0 and not force:
        conn.close()
        return
```

**防止重复导入**：如果分类表已有数据（count > 0），且没有强制导入（force=False），则跳过。

```python
    with open(PHRASES_JSON, "r", encoding="utf-8") as f:
        data = json.load(f)
```

- `open(..., "r", encoding="utf-8")`：以只读方式打开 JSON 文件
- `json.load(f)`：把 JSON 文件内容解析为 Python 对象（列表嵌套字典）

```python
    seen_categories = {}
    for item in data:
        cat_name = item["category"]
```

**遍历 JSON 数据**：
- `seen_categories = {}`：空字典，记录已创建的分类
- `item`：JSON 数组中的每个元素，结构为 `{"category": "日常问候", "phrases": [...]}`

```python
        if cat_name not in seen_categories:
            conn.execute(
                "INSERT OR IGNORE INTO categories (name, description) VALUES (?, ?)",
                (cat_name, cat_name),
            )
            row = conn.execute(
                "SELECT id FROM categories WHERE name = ?", (cat_name,)
            ).fetchone()
            seen_categories[cat_name] = row["id"]
```

**插入分类并获取 ID**：
- `INSERT OR IGNORE`：如果分类名已存在（UNIQUE 约束），忽略本次插入
- `(?, ?)`：SQL 参数占位符。**永远不要用字符串拼接 SQL！** 用 `?` 占位符防止 SQL 注入
- `(cat_name, cat_name)`：对应两个 `?`，分类名同时作为 name 和 description
- `SELECT id FROM categories WHERE name = ?`：查询刚插入的分类的 ID
- `seen_categories[cat_name] = row["id"]`：将分类名 → ID 的映射存入字典

```python
        cat_id = seen_categories[cat_name]
        for phrase in item.get("phrases", []):
            conn.execute(
                "INSERT OR IGNORE INTO phrases (category_id, en, zh) VALUES (?, ?, ?)",
                (cat_id, phrase["en"], phrase.get("zh", "")),
            )
```

- `item.get("phrases", [])`：从当前分类中获取短语列表，如果不存在则返回空列表
- `phrase["en"]`：英文
- `phrase.get("zh", "")`：中文，如果不存在则默认为空字符串

---

### 3.3 `app/models.py` — 数据模型

```python
from pydantic import BaseModel
from typing import Optional
```

**Pydantic**：FastAPI 集成的数据校验库。通过定义 Python 类，自动校验 JSON 请求和响应数据。

**`BaseModel`**：Pydantic 的基类，所有模型都要继承它。

**`Optional`**：表示参数可以为 None（即 JSON 中可以不传）。

---

#### CategoryOut — 分类输出

```python
class CategoryOut(BaseModel):
    id: int
    name: str
    description: Optional[str] = None
    phrase_count: int = 0
```

- `id: int`：分类 ID，整数类型
- `name: str`：分类名称，字符串
- `description: Optional[str] = None`：描述，可选，默认 None
- `phrase_count: int = 0`：该分类下的短语数量，默认 0

> **`= None`** vs **`= 0`**：如果前端返回的 JSON 中不包含这个字段，Optional 字段取 None，普通字段取默认值。

---

#### PhraseOut — 短语输出

```python
class PhraseOut(BaseModel):
    id: int
    category_id: int
    en: str
    zh: Optional[str] = None
    phonetic: Optional[str] = None
    example: Optional[str] = None
    type: str = "phrase"
    audio_file: Optional[str] = None
    status: Optional[str] = "new"
    correct_count: int = 0
    wrong_count: int = 0
    last_review: Optional[int] = None
    next_review: Optional[int] = None
```

**字段说明**：
- `type: str = "phrase"`：内容类型，默认 "phrase"。可选值：word（单词）、phrase（短句）、passage（短文）
- `audio_file: Optional[str] = None`：关联的音频文件名，如 "1.mp3" 或 "rec_5.mp3"
- `status: Optional[str] = "new"`：学习状态（来自 progress 表）
- `correct_count / wrong_count`：连续正确/错误次数
- `last_review / next_review`：上次/下次复习时间戳

---

#### PhraseCreate / PhraseUpdate — 创建/更新请求

```python
class PhraseCreate(BaseModel):
    category_id: int
    en: str
    zh: Optional[str] = None
    type: str = "phrase"
    audio_file: Optional[str] = None

class PhraseUpdate(BaseModel):
    en: Optional[str] = None
    zh: Optional[str] = None
    type: Optional[str] = None
    audio_file: Optional[str] = None
```

**区别**：
- `PhraseCreate`：category_id 和 en 是**必填**的（没有默认值），zh 和 audio_file 可选
- `PhraseUpdate`：所有字段都是**可选**的（Partial Update），只更新传了的字段

---

#### ProgressUpdate — 进度更新请求

```python
class ProgressUpdate(BaseModel):
    is_correct: bool
```

只有一个字段：`is_correct`，布尔值，表示本次答题是否正确。

---

#### StatsOut — 统计输出

```python
class StatsOut(BaseModel):
    total_phrases: int = 0
    total_answers: int = 0
    correct_answers: int = 0
    wrong_answers: int = 0
    accuracy: float = 0.0
    mastered: int = 0
    learning: int = 0
    new_count: int = 0
```

**计算方式**：
- `total_phrases`：所有短语数量
- `total_answers = correct_answers + wrong_answers`：总答题数
- `accuracy = correct_answers / total_answers * 100`：正确率
- `mastered`：已掌握数量（status='mastered'）
- `learning`：学习中数量（status='learning'）
- `new_count = total_phrases - mastered - learning`：未学习数量

---

### 3.4 `app/routers/categories.py` — 分类接口

```python
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from typing import Optional
from app.database import get_connection
from app.models import CategoryOut

router = APIRouter()
```

**`APIRouter()`**：创建路由对象，可以在不同文件定义路由，然后在 main.py 统一注册。

**`Depends`**：FastAPI 的依赖注入系统。可以自动把函数返回值注入到路由函数的参数中。

---

#### `list_categories()` — 获取所有分类

```python
@router.get("/categories", response_model=list[CategoryOut])
def list_categories(conn=Depends(get_connection)):
    rows = conn.execute("""
        SELECT c.*, COUNT(p.id) as phrase_count
        FROM categories c
        LEFT JOIN phrases p ON c.id = p.category_id
        GROUP BY c.id
        ORDER BY c.id
    """).fetchall()
    return [dict(r) for r in rows]
```

**装饰器解释**：
- `@router.get("/categories")`：注册 GET 请求，路径为 /categories（加上前缀后为 /api/categories）
- `response_model=list[CategoryOut]`：告诉 FastAPI 返回的数据类型是 CategoryOut 的列表，自动校验和文档生成

**参数解释**：
- `conn=Depends(get_connection)`：FastAPI 调用 `get_connection()` 获取数据库连接，并注入到 conn 参数。每次请求自动创建新连接，请求结束自动关闭

**SQL 详解**：
```sql
SELECT c.*, COUNT(p.id) as phrase_count
FROM categories c
LEFT JOIN phrases p ON c.id = p.category_id
GROUP BY c.id
ORDER BY c.id
```
- `c.*`：categories 表的所有列
- `COUNT(p.id) as phrase_count`：统计每个分类下的短语数量
- `LEFT JOIN`：左连接，即使分类下没有短语也会显示（phrase_count = 0）
- `GROUP BY c.id`：按分类 ID 分组
- `ORDER BY c.id`：按 ID 升序排列

**`fetchall()`**：获取所有查询结果，返回 Row 对象列表
**`[dict(r) for r in rows]`**：列表推导式，把每个 Row 对象转成字典

---

#### `create_category()` — 新建分类

```python
@router.post("/categories", response_model=CategoryOut)
def create_category(body: CategoryCreate, conn=Depends(get_connection)):
    try:
        cur = conn.execute(
            "INSERT INTO categories (name, description) VALUES (?, ?)",
            (body.name, body.description or body.name),
        )
        conn.commit()
    except Exception as e:
        raise HTTPException(400, f"创建失败（可能名称重复）: {e}")
```

- `@router.post(...)`：处理 POST 请求
- `body: CategoryCreate`：FastAPI 从请求体中解析 JSON，校验类型（要求有 name 字段），转为 CategoryCreate 对象
- `body.description or body.name`：如果 description 为空，用 name 替代
- `cur = conn.execute(...)`：执行插入，返回 cursor 对象
- `conn.commit()`：提交事务，SQLite 才真正写入文件
- `except Exception as e`：捕获所有异常
- `HTTPException(400, ...)`：返回 HTTP 400 错误，附带错误信息

```python
    row = conn.execute("""
        SELECT c.*, COUNT(p.id) as phrase_count
        FROM categories c
        LEFT JOIN phrases p ON c.id = p.category_id
        WHERE c.id = ?
        GROUP BY c.id
    """, (cur.lastrowid,)).fetchone()
    return dict(row)
```

- `cur.lastrowid`：获取刚插入记录的 ID
- `WHERE c.id = ?`：根据 ID 查询
- `fetchone()`：获取一行结果
- `return dict(row)`：返回字典，FastAPI 自动转为 JSON

---

#### `update_category()` / `delete_category()`

`update_category`：根据 id 更新分类的 name 和 description。如果分类不存在返回 404。

`delete_category`：根据 id 删除分类。由于建表时设置了 `ON DELETE CASCADE`，该分类下的短语也会自动删除。

---

### 3.5 `app/routers/phrases.py` — 短语接口

```python
AUDIO_DIR = Path(__file__).resolve().parent.parent.parent / "public" / "audio"
AUDIO_DIR.mkdir(parents=True, exist_ok=True)
```

**音频目录**：从 app/routers/ 上三级到项目根目录，再拼接 public/audio。`mkdir` 确保目录存在。

```python
SELECT_COLS = """
    SELECT p.id, p.category_id, p.en, p.zh, p.phonetic, p.example,
           COALESCE(p.type, 'phrase') as type,
           p.audio_file,
           COALESCE(pr.status, 'new') as status,
           COALESCE(pr.correct_count, 0) as correct_count,
           COALESCE(pr.wrong_count, 0) as wrong_count,
           pr.last_review, pr.next_review
    FROM phrases p
    LEFT JOIN progress pr ON p.id = pr.phrase_id
"""
```

**SQL 片段**：多条路由复用同一个查询。用**多行字符串**定义。

- `COALESCE(p.type, 'phrase')`：如果 p.type 为 NULL，用 'phrase' 替代
- `COALESCE(pr.status, 'new')`：如果 progress 表中没有记录（新短语），状态默认为 'new'
- `LEFT JOIN progress pr`：左连接 progress 表，获取学习进度

---

#### `list_phrases()` — 获取短语列表

```python
@router.get("/phrases", response_model=list[PhraseOut])
def list_phrases(
    category_id: int = Query(None),
    conn=Depends(get_connection),
):
```

**`Query(None)`**：查询参数，可选。如果传了 `/api/phrases?category_id=1`，则只返回该分类下的短语。

```python
    if category_id:
        rows = conn.execute(SELECT_COLS + " WHERE p.category_id = ? ORDER BY p.id", (category_id,)).fetchall()
    else:
        rows = conn.execute(SELECT_COLS + " ORDER BY p.id").fetchall()
    return [dict(r) for r in rows]
```

根据是否有 category_id 参数拼接不同的 WHERE 条件。

---

#### `create_phrase()` — 新建短语

```python
@router.post("/phrases", response_model=PhraseOut)
def create_phrase(body: PhraseCreate, conn=Depends(get_connection)):
    cur = conn.execute(
        "INSERT INTO phrases (category_id, en, zh, type, audio_file) VALUES (?, ?, ?, ?, ?)",
        (body.category_id, body.en, body.zh, body.type, body.audio_file),
    )
    conn.commit()
    row = conn.execute(SELECT_COLS + " WHERE p.id = ?", (cur.lastrowid,)).fetchone()
    return dict(row)
```

简单插入后查询返回完整数据。

---

#### `update_phrase()` — 更新短语（动态 SQL）

```python
@router.put("/phrases/{phrase_id}", response_model=PhraseOut)
def update_phrase(phrase_id: int, body: PhraseUpdate, conn=Depends(get_connection)):
    fields = {}
    for key in ("en", "zh", "type", "audio_file"):
        val = getattr(body, key, None)
        if val is not None:
            fields[key] = val
    if not fields:
        raise HTTPException(400, "没有要更新的字段")
    set_clause = ", ".join(f"{k} = ?" for k in fields)
    values = list(fields.values()) + [phrase_id]
    conn.execute(f"UPDATE phrases SET {set_clause} WHERE id = ?", values)
    conn.commit()
    row = conn.execute(SELECT_COLS + " WHERE p.id = ?", (phrase_id,)).fetchone()
    if not row:
        raise HTTPException(404, "短语不存在")
    return dict(row)
```

**动态 SQL 技巧**：
- `{phrase_id}`：URL 路径参数，如 `/api/phrases/5`
- `fields = {}`：空字典
- `getattr(body, key, None)`：反射方式获取对象的 key 属性，如果不存在返回 None
- `if val is not None`：只更新前端传了的字段（非 None）
- `set_clause = ", ".join(f"{k} = ?" for k in fields)`：生成 "en = ?, zh = ?, type = ?" 这样的字符串
- `values = list(fields.values()) + [phrase_id]`：拼接值和 ID

---

#### `upload_audio()` — 上传音频

```python
@router.post("/upload-audio")
async def upload_audio(file: UploadFile = File(...)):
    if not file.filename.lower().endswith(".mp3"):
        raise HTTPException(400, "仅支持 MP3 文件")
    file_path = AUDIO_DIR / file.filename
    content = await file.read()
    file_path.write_bytes(content)
    return {"filename": file.filename, "path": f"/audio/{file.filename}"}
```

- `async def`：异步函数（因为文件读写是 IO 操作）
- `file: UploadFile = File(...)`：FastAPI 自动解析上传的文件
- `file.filename`：上传时的原始文件名
- `await file.read()`：异步读取文件内容
- `file_path.write_bytes(content)`：写入到 audio 目录

---

#### `review_phrases()` — 获取复习列表

```python
@router.get("/review", response_model=list[PhraseOut])
def review_phrases(conn=Depends(get_connection)):
    now = int(__import__("time").time() * 1000)
    rows = conn.execute(SELECT_COLS + """
        WHERE pr.status IS NULL
           OR pr.status = 'learning'
           OR (pr.status = 'mastered' AND pr.next_review IS NOT NULL AND pr.next_review <= ?)
        ORDER BY p.id
    """, (now,)).fetchall()
    return [dict(r) for r in rows]
```

**复习逻辑**：
- `__import__("time").time() * 1000`：动态导入 time 模块，获取当前时间戳（毫秒）
- 显示需要复习的短语：未学习（pr.status IS NULL）或学习中（learning）或已掌握但到期（mastered 且 next_review <= 当前时间）

---

### 3.6 `app/routers/progress.py` — 进度接口

#### `update_progress()` — 更新进度

```python
@router.post("/progress/{phrase_id}")
def update_progress(phrase_id: int, body: ProgressUpdate, conn=Depends(get_connection)):
    row = conn.execute(
        "SELECT * FROM progress WHERE phrase_id = ?", (phrase_id,)
    ).fetchone()
```

- 查询该短语现有的进度记录

```python
    status = "new"
    correct_count = 0
    wrong_count = 0
    last_review = int(__import__("time").time() * 1000)
```

初始化默认值。

```python
    if row:
        status = row["status"] or "new"
        correct_count = row["correct_count"] or 0
        wrong_count = row["wrong_count"] or 0
```

如果有现有记录，读取其中的值。

```python
    if body.is_correct:
        correct_count += 1
        wrong_count = 0
        if correct_count >= 3:
            status = "mastered"
        else:
            status = "learning"
    else:
        wrong_count += 1
        correct_count = 0
        status = "learning"
```

**核心进度算法**（称为"连续正确法"）：
- 答对：correct_count +1，wrong_count 清零
  - 如果 correct_count >= 3 → 标记为已掌握（mastered）
  - 否则 → 学习中（learning）
- 答错：wrong_count +1，correct_count 清零
  - 标记为学习中（learning）（不重置为新）

```python
    next_review = last_review + (24 * 60 * 60 * 1000 if status == "mastered" else 0)
```

- 已掌握：24 小时后复习（24h × 60min × 60s × 1000ms）
- 学习中：立即复习（+0）

```python
    conn.execute("""
        INSERT INTO progress (phrase_id, status, correct_count, wrong_count, last_review, next_review)
        VALUES (?, ?, ?, ?, ?, ?)
        ON CONFLICT(phrase_id) DO UPDATE SET
            status = excluded.status,
            correct_count = excluded.correct_count,
            wrong_count = excluded.wrong_count,
            last_review = excluded.last_review,
            next_review = excluded.next_review
    """, (phrase_id, status, correct_count, wrong_count, last_review, next_review))
```

**UPSERT（INSERT + UPDATE）**：
- 尝试插入新记录
- `ON CONFLICT(phrase_id)`：如果 phrase_id 已存在（UNIQUE 约束冲突）
- `DO UPDATE SET ... = excluded.xxx`：用新值更新已有记录。`excluded` 指 VALUES 中的值

```python
    conn.execute("""
        UPDATE stats SET
            total_answers = total_answers + 1,
            correct_answers = correct_answers + ?,
            wrong_answers = wrong_answers + ?
        WHERE id = 1
    """, (1 if body.is_correct else 0, 0 if body.is_correct else 1))
```

更新统计表。正确则 correct_answers +1，错误则 wrong_answers +1。

---

### 3.7 `app/routers/stats.py` — 统计接口

```python
@router.get("/stats", response_model=StatsOut)
def get_stats(conn=Depends(get_connection)):
    total_phrases = conn.execute("SELECT COUNT(*) FROM phrases").fetchone()[0]
    row = conn.execute("SELECT * FROM stats WHERE id = 1").fetchone()
    total_answers = row["total_answers"] if row else 0
    correct_answers = row["correct_answers"] if row else 0
    wrong_answers = row["wrong_answers"] if row else 0
    mastered = conn.execute("SELECT COUNT(*) FROM progress WHERE status = 'mastered'").fetchone()[0]
    learning = conn.execute("SELECT COUNT(*) FROM progress WHERE status = 'learning'").fetchone()[0]
    new_count = total_phrases - mastered - learning
    accuracy = round((correct_answers / total_answers * 100), 1) if total_answers > 0 else 0.0
    return StatsOut(...)
```

**SQL COUNT**：`SELECT COUNT(*) FROM ...` 返回满足条件的行数。`fetchone()[0]` 取第一列的值。

---

### 3.8 `app/routers/audio.py` — 音频接口

#### `record_audio()` — 录制并转 MP3

```python
@router.post("/record-audio/{phrase_id}")
async def record_audio(phrase_id: int, file: UploadFile = File(...)):
    content = await file.read()
    suffix = Path(file.filename).suffix or ".webm"
    temp_name = f"temp_{phrase_id}_{int(time.time())}{suffix}"
    temp_path = AUDIO_DIR / temp_name
```

- 从浏览器接收录制的音频 blob
- 获取文件后缀名（如 .webm），如果没有则默认为 .webm
- 创建临时文件名：`temp_{phrase_id}_{当前时间戳}.webm`

```python
    try:
        temp_path.write_bytes(content)
```

将上传的二进制数据写入临时文件。

```python
        from pydub import AudioSegment
        mp3_filename = f"rec_{phrase_id}.mp3"
        mp3_path = AUDIO_DIR / mp3_filename

        audio = AudioSegment.from_file(str(temp_path))
        audio.export(str(mp3_path), format="mp3")
```

**pydub 音频转换**：
- `AudioSegment.from_file()`：读取音频文件（自动识别格式）
- `audio.export(..., format="mp3")`：导出为 MP3 格式（底层调用 ffmpeg）
- 输出文件名：`rec_{phrase_id}.mp3`，如 `rec_5.mp3`

```python
        conn = get_connection()
        conn.execute(
            "UPDATE phrases SET audio_file = ? WHERE id = ?",
            (mp3_filename, phrase_id),
        )
        conn.commit()
        conn.close()
```

- 打开数据库连接
- 更新短语的 audio_file 字段
- 提交并关闭连接

```python
        return {"filename": mp3_filename, "path": f"/audio/{mp3_filename}"}
    except Exception as e:
        raise HTTPException(500, f"音频处理失败: {str(e)}")
    finally:
        if temp_path.exists():
            temp_path.unlink()
```

- `return`：成功后返回文件名和访问路径
- `except`：任何异常返回 500 错误
- `finally`：**无论成功还是失败**，都删除临时文件

---

## 四、前端代码详解

### 4.1 `public/index.html` — 学习页面

#### HTML 结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>英语学习 V5</title>
    <link rel="stylesheet" href="src/styles.css">
</head>
```

- `<!DOCTYPE html>`：声明 HTML5 文档类型
- `lang="zh-CN"`：页面语言为简体中文
- `<meta charset="UTF-8">`：使用 UTF-8 编码，支持中文
- `viewport`：移动端适配
- `<link rel="stylesheet" href="src/styles.css">`：加载 CSS

```html
<body>
<div class="app-layout">
    <div class="main-area">
```

- `.app-layout`：flex 布局容器，包含主区域和侧边栏
- `.main-area`：主区域（卡片和测试），`flex: 1` 占满剩余空间

```html
<button class="toggle-settings" id="toggleSettings">&#x2630;</button>
```

**☰ 汉堡菜单按钮**：`&#x2630`; 是 "☰" 的 HTML 实体编码。点击打开右侧设置面板。

```html
<div id="learnSection">
    <div class="card-main">
        <button class="nav-arrow nav-prev" onclick="prevCard()">&#x2039;</button>
        <button class="nav-arrow nav-next" onclick="nextCard()">&#x203A;</button>
```

**卡片导航**：左右箭头按钮，点击切换上一张/下一张卡片。`&#x2039`; 和 `&#x203A`; 分别是 ‹ 和 ›。

```html
<div class="card" id="flashcard">
    <div class="card-face card-front">
        <div class="card-content">
            <div class="card-english" id="cardEnglish">加载中...</div>
            <div class="card-bottom-row">
                <div class="card-status" id="cardStatus"></div>
                <button class="sound-btn" onclick="playSound(event)">&#x1F50A;</button>
            </div>
        </div>
    </div>
    <div class="card-face card-back">
        <div class="card-content">
            <div class="card-chinese" id="cardChinese"></div>
            <div class="card-bottom-row">
                <div class="card-status" id="cardStatusBack"></div>
                <button class="sound-btn" onclick="playSound(event)">&#x1F50A;</button>
            </div>
        </div>
    </div>
</div>
```

**翻转卡片**：
- `.card`：容器，点击切换 `flipped` 类，触发 CSS 3D 翻转
- `.card-front`：正面，显示英文和 🔊 按钮
- `.card-back`：背面，显示中文和 🔊 按钮。CSS 设置 `transform: rotateY(180deg)`
- `backface-visibility: hidden`：隐藏背面
- `&#x1F50A`;：🔊 喇叭 emoji

```html
<div id="testSection">
    <div class="test-word" id="testWord">加载中...</div>
    <div class="test-hint">输入单词后按空格，逐词检测</div>
    <div class="input-row">
        <input type="text" class="test-input" id="testInput" placeholder="输入英文..." autocomplete="off">
        <div class="test-result" id="testResult"></div>
    </div>
    ...
</div>
```

**测试区域**：默认隐藏（`display: none`），切换测试模式时显示（`display: flex`）。

#### 设置侧边栏

```html
<div class="settings-sidebar" id="settingsPanel">
```

**侧边栏**：
- 默认 `width: 0`（隐藏）
- 点击 ☰ 添加 `open` 类 → `width: 380px`（动画展开）

侧边栏包含：
- **统计栏**：总短句、已掌握、学习中、正确率
- **分类筛选**：全部、各分类按钮
- **模式切换**：学习 / 测试 / 复习
- **等级过滤**：未学习 / 学习中 / 已掌握（复选框）
- **重置进度按钮**、**后台管理链接**

---

### 4.2 `public/src/styles.css` — 学习页面样式

#### 通用重置

```css
* { margin: 0; padding: 0; box-sizing: border-box; }
```

- `*`：选中所有元素
- `margin: 0; padding: 0`：清除默认边距
- `box-sizing: border-box`：宽度包含 padding 和 border（更易布局）

#### 页面背景

```css
html, body {
    height: 100%;
    overflow: hidden;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
}
```

- `height: 100%`：占满视口
- `overflow: hidden`：禁止滚动
- `linear-gradient(135deg, ...)`：深蓝紫色渐变背景

#### 卡片翻转动画

```css
.card-container {
    perspective: 1200px;
}

.card {
    transition: transform 0.6s;
    transform-style: preserve-3d;
}

.card.flipped { transform: rotateY(180deg); }

.card-face {
    backface-visibility: hidden;
}

.card-back {
    transform: rotateY(180deg);
}
```

**3D 翻转原理**：
- `perspective: 1200px`：透视距离，让 3D 效果更真实
- `transform-style: preserve-3d`：保留子元素的 3D 变换
- `transition: transform 0.6s`：0.6 秒过渡动画
- `card-back` 初始旋转 180°，所以 backface-visibility 让它隐藏
- 添加 `flipped` 类 → 整个卡片旋转 180° → 背面转到正面 → 显示中文

---

### 4.3 `public/src/app.js` — 学习页面逻辑

#### API 对象

```javascript
const API = {
    async getCategories() {
        const res = await fetch('/api/categories');
        return await res.json();
    },
    ...
};
```

**`const API = { ... }`**：定义一个对象，把所有的 API 调用封装在一起，方便管理和复用。

**`async/await`**：JavaScript 处理异步操作的关键词。
- `async`：声明一个异步函数
- `await`：等待一个异步操作完成（会暂停函数执行，但不会阻塞浏览器）
- `fetch()`：浏览器内置的 HTTP 请求函数，返回 Promise

**完整流程**：`fetch('/api/categories')` → 发送 GET 请求 → 等待响应 → `res.json()` 解析 JSON → 返回数据。

#### 全局变量

```javascript
let phrases = [];
let categories = [];
let progress = {};
let currentIndex = 0;
let currentMode = 'learn';
let visiblePhrases = [];
let filteredStatus = ['new', 'learning', 'mastered'];
let selectedCategory = null;
```

- `phrases`：当前分类下的所有短语数组
- `categories`：所有分类数组
- `progress`：进度对象，key 是短语 ID，value 是 `{status, correct_count, wrong_count}`
- `currentIndex`：当前显示的卡片索引
- `currentMode`：当前模式（'learn' / 'test' / 'review'）
- `visiblePhrases`：经过筛序后显示的子集
- `filteredStatus`：勾选的等级过滤条件
- `selectedCategory`：选中的分类 ID

#### `init()` — 初始化入口

```javascript
async function init() {
    await loadCategories();
    await loadData();
    updateVisiblePhrases();
    updateStats();
    updateCard();
    setupEventListeners();
}
```

**启动顺序**：
1. `loadCategories()`：加载分类列表
2. `loadData()`：加载短语列表
3. `updateVisiblePhrases()`：根据筛选条件更新可见短语
4. `updateStats()`：更新统计信息
5. `updateCard()`：显示当前卡片
6. `setupEventListeners()`：绑定事件监听

#### `loadCategories()` — 加载分类

```javascript
async function loadCategories() {
    categories = await API.getCategories();
    const categorySelect = document.getElementById('categorySelect');
    categorySelect.innerHTML = '<button class="cat-btn active" data-id="">全部</button>';
    categories.forEach(cat => {
        const btn = document.createElement('button');
        btn.className = 'cat-btn';
        btn.dataset.id = cat.id;
        btn.textContent = cat.name;
        categorySelect.appendChild(btn);
    });
}
```

**`document.createElement('button')`**：用 JS 创建 HTML 元素，比拼字符串更安全（避免 XSS 攻击）。

**`btn.dataset.id = cat.id`**：`data-id` 属性的 JS 对应写法。HTML 中变成 `<button data-id="3">`。

**`categorySelect.appendChild(btn)`**：将按钮添加到容器中。

#### `updateVisiblePhrases()` — 筛选可见短语

```javascript
function updateVisiblePhrases() {
    visiblePhrases = phrases.filter(phrase => {
        const p = progress[phrase.id] || { status: 'new' };
        return filteredStatus.includes(p.status);
    });
    if (visiblePhrases.length === 0) {
        visiblePhrases = phrases;
    }
    if (currentIndex >= visiblePhrases.length) {
        currentIndex = 0;
    }
}
```

- `phrases.filter(...)`：数组过滤，返回满足条件的子数组
- `progress[phrase.id]`：从 progress 对象中取该短语的进度
- `|| { status: 'new' }`：如果没有进度记录，默认为 'new'
- `filteredStatus.includes(p.status)`：检查短语状态是否在筛选条件中
- 如果筛选后为空，显示全部（`visiblePhrases = phrases`）

#### `updateCard()` — 渲染卡片

```javascript
function updateCard() {
    if (visiblePhrases.length === 0) {
        cardEnglish.textContent = '暂无短句';
        cardChinese.textContent = '';
        return;
    }
    const phrase = visiblePhrases[currentIndex];
    const p = progress[phrase.id] || { status: 'new' };
    ...
}
```

- `textContent`：设置元素的文本内容（比 innerHTML 更安全）
- 从 `visiblePhrases[currentIndex]` 取当前要显示的短语

#### `playAudio()` / `playSound()` — 播放声音

```javascript
function playAudio(phraseId, text, audioFile) {
    const audioPath = audioFile ? '/audio/' + audioFile : '/audio/' + phraseId + '.mp3';
    const audio = new Audio(audioPath);
    audio.onerror = function () {
        if (!('speechSynthesis' in window)) return;
        const utterance = new SpeechSynthesisUtterance(text);
        utterance.lang = 'en-US';
        utterance.rate = 0.8;
        speechSynthesis.speak(utterance);
    };
    audio.play();
}
```

**双重播放机制**：
1. 优先播放音频文件（如果有关联的 mp3）
   - `new Audio(audioPath)`：创建 Audio 对象
   - `audio.play()`：播放
2. 如果音频文件不存在或加载失败（onerror）
   - 用浏览器内置的**语音合成**（SpeechSynthesis）朗读
   - `SpeechSynthesisUtterance(text)`：创建朗读对象
   - `utterance.lang = 'en-US'`：美式英语发音
   - `utterance.rate = 0.8`：语速 80%

#### 测试模式 — `displayTestPhrase()`

```javascript
function displayTestPhrase() {
    const phrase = visiblePhrases[currentIndex];
    testWord.textContent = phrase.zh || '';   // 显示中文
    testInput.value = '';                     // 清空输入框
    testResult.className = 'test-result';      // 隐藏结果
    completedWords = [];                       // 清空已完成的单词
    wordDisplay.innerHTML = '';                // 清空单词展示
    testInput.focus();                         // 聚焦输入框
}
```

#### 键盘输入检测

```javascript
testInput.addEventListener('keydown', (e) => {
    if (e.key === ' ') {          // 按空格：提交当前输入的单词
        e.preventDefault();       // 阻止默认行为（页面滚动）
        const raw = testInput.value.trim();
        if (!raw) {               // 空字符串：连续3次空格触发"检查答案"
            emptySpaceCount++;
            if (emptySpaceCount >= 3) checkAnswer();
            return;
        }
        const phrase = visiblePhrases[currentIndex];
        const correctWords = phrase.en.toLowerCase().split(/\s+/).map(stripPunct);
        const cleaned = stripPunct(raw.toLowerCase());
        const match = idx < correctWords.length && cleaned === correctWords[idx];
        completedWords.push({ word: raw, match });
        renderWordDisplay();      // 显示已输入的单词（绿色正确/红色错误）
        testInput.value = '';     // 清空输入框，继续下一个单词
    }
    if (e.key === 'Backspace' && testInput.value === '' && completedWords.length > 0) {
        // 退格键回退到上一个单词
        const last = completedWords.pop();
        testInput.value = last.word;
    }
    if (e.key === 'Enter') {      // 回车：检查整个答案
        checkAnswer();
    }
});
```

**逐词检测原理**：
- 用户输入英文，按空格提交一个词
- 与正确短语的对应位置单词比较（忽略大小写和标点）
- 正确显示绿色，错误显示红色+删除线
- 全部输入完成后按回车判断整体是否正确

#### `checkAnswer()` — 检查答案

```javascript
async function checkAnswer() {
    const phrase = visiblePhrases[currentIndex];
    const correctWords = phrase.en.toLowerCase().split(/\s+/).map(stripPunct);

    let allCorrect = completedWords.length === correctWords.length;
    for (let i = 0; i < completedWords.length; i++) {
        if (!completedWords[i].match) allCorrect = false;
    }

    await API.updateProgress(phrase.id, allCorrect);
    await loadData();
    updateStats();
    ...
}
```

- 比较单词数量是否一致且每个都匹配
- 调用 API 更新进度
- 重新加载数据（获取最新状态）
- 更新统计信息

---

### 4.4 `public/admin.html` — 后台管理页面

**布局**：
- 左侧侧边栏：分类列表 + 音频管理
- 右侧主区域：内容列表（带搜索和筛选）+ 模态框（新增/编辑表单）

**模态框结构**：
```html
<div class="modal-overlay" id="modal">
    <div class="modal">
        <h2 id="modalTitle">添加内容</h2>
        <div class="modal-body" id="modalBody"></div>
        <div class="modal-actions">
            <button class="btn-primary" onclick="submitForm()">保存</button>
            <button class="btn-cancel" onclick="closeModal()">取消</button>
        </div>
    </div>
</div>
```

- `.modal-overlay`：半透明黑色遮罩
- `.modal`：白色弹窗
- `modalBody` 的内容由 JS 动态生成

---

### 4.5 `public/src/admin.js` — 后台管理逻辑

#### `formHtml(p)` — 生成表单 HTML

```javascript
function formHtml(p) {
    const t = p ? p.type : 'phrase';
    const en = p ? p.en : '';
    const zh = p ? p.zh : '';
    const catId = p ? p.category_id : (selectedCatId || '');
    const audioStatus = p?.audio_file ? `当前音频: ${p.audio_file}` : '';
    const catOpts = categories.map(c =>
        `<option value="${c.id}" ${c.id === catId ? 'selected' : ''}>${esc(c.name)}</option>`
    ).join('');
    return `...模板字符串...`;
}
```

**函数参数**：
- `p`：短语对象（编辑时）或 null（新建时）
- 根据 `p` 是否为 null 决定表单默认值

**`categories.map(...).join('')`**：遍历所有分类，生成 `<option>` 字符串数组，再拼接成一个字符串。

**模板字符串**用反引号 ` `` ` 包裹，`${...}` 内嵌 JavaScript 表达式。

#### 音频录制按钮

```javascript
<button type="button" class="btn-record" id="adminRecordBtn"
    onclick="toggleAdminRecording()" ${!p ? 'disabled' : ''}>🎤 录制</button>
```

- 编辑时（p 存在）：按钮可用
- 新建时（p 为 null）：按钮禁用（disabled）

#### `toggleAdminRecording()` — 录音功能

```javascript
async function toggleAdminRecording() {
    if (!editingId) {
        alert('请先保存内容，然后编辑时再录制');
        return;
    }
```

检查是否有短语 ID（编辑状态下才有）。

```javascript
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
```

**`navigator.mediaDevices.getUserMedia({ audio: true })`**：请求浏览器麦克风权限，返回音频流。用户会看到权限弹窗。

```javascript
    const mime = MediaRecorder.isTypeSupported('audio/webm;codecs=opus')
        ? 'audio/webm;codecs=opus' : 'audio/webm';
    adminMediaRecorder = new MediaRecorder(stream, { mimeType: mime });
```

- `MediaRecorder.isTypeSupported(...)`：检测浏览器是否支持某种音频编码
- `MediaRecorder`：浏览器内置的录音 API

```javascript
    adminMediaRecorder.ondataavailable = e => {
        if (e.data.size > 0) adminRecordingChunks.push(e.data);
    };
```

**数据回调**：每当录音有数据可用时，将数据块存入数组。

```javascript
    adminMediaRecorder.onstop = async () => {
        stream.getTracks().forEach(t => t.stop());  // 关闭麦克风
        const blob = new Blob(adminRecordingChunks, { type: adminMediaRecorder.mimeType });
        const fd = new FormData();
        fd.append('file', blob, `rec_${editingId}.webm`);

        const res = await fetch(`/api/record-audio/${editingId}`, { method: 'POST', body: fd });
        ...
    };
```

**停止录音时**：
1. 关闭所有麦克风轨道（释放资源）
2. 将所有音频块合并为一个 Blob
3. 创建 FormData 发送到后端
4. 后端自动转 MP3 并关联到短语

```javascript
    adminMediaRecorder.start();
    adminIsRecording = true;
```

开始录音。

---

### 4.6 CSS 样式解析

#### flexbox 布局核心

```css
.admin-layout { display: flex; }
.sidebar { width: 240px; flex-shrink: 0; }
.main-content { flex: 1; }
```

- 侧边栏固定宽度 240px，不缩小（`flex-shrink: 0`）
- 主区域 `flex: 1`，占满剩余空间

#### 模态框居中

```css
.modal-overlay {
    display: none;
    position: fixed;
    inset: 0;
    align-items: center;
    justify-content: center;
}
.modal-overlay.active { display: flex; }
```

- `position: fixed`：固定在视口
- `inset: 0`：top/right/bottom/left 全为 0
- `display: flex` + `align-items/justify-content: center`：完美居中

#### CSS 脉冲动画

```css
.btn-record.recording {
    background: #e74c3c;
    animation: admin-pulse 1s ease-in-out infinite;
}
@keyframes admin-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(231,76,60,0.5); }
    50% { box-shadow: 0 0 0 6px rgba(231,76,60,0); }
}
```

**录制时红色脉冲效果**：
- 1 秒循环一次
- 阴影半径从 0 到 6px 再回 0

---

## 五、数据流实战

### 5.1 学习流程

```
用户打开页面
  ↓
浏览器请求 index.html + app.js + styles.css
  ↓
app.js 的 init() 执行
  ↓
  loadCategories() → GET /api/categories → SQLite 查询 → 返回 [{"id":1,"name":"日常问候",...}]
  ↓
  loadData() → GET /api/phrases → SQLite 查询（LEFT JOIN progress）
    → 返回 [{"id":1,"en":"Good morning!","status":"new","correct_count":0,...}]
  ↓
  updateVisiblePhrases() → 根据 filteredStatus 过滤
  ↓
  updateCard() → 显示第一个短语
  ↓
用户点击卡片 → card.classList.toggle('flipped') → CSS 3D 翻转显示中文
  ↓
用户点击 🔊 按钮 → playSound()
  ↓
  playAudio(phrase.id, phrase.en, phrase.audio_file)
  → 如果有 audio_file → 播放 /audio/rec_1.mp3
  → 如果没有 → 播放 /audio/1.mp3
  → 如果文件也不存在 → speechSynthesis 朗读
```

### 5.2 测试流程

```
用户切换到测试模式
  ↓
displayTestPhrase() → 显示中文
  ↓
用户逐词输入英文，按空格提交每个词
  ↓
按 Enter 或 3次空格 → checkAnswer()
  ↓
  API.updateProgress(phraseId, allCorrect)
  → POST /api/progress/{id}
  → 后端计算新状态（连续3次正确=已掌握）
  → 更新 progress 表和 stats 表
  ↓
显示 ✓ 或 ✗，3秒后自动下一个
```

### 5.3 录音流程

```
后台管理 → 点击短语的"编辑"
  ↓
showEditForm(id) → 设置 editingId = id
  ↓
formHtml(p) → 生成表单，包含 🎤 录制按钮
  ↓
用户点击 🎤 录制
  ↓
toggleAdminRecording()
  ↓
navigator.mediaDevices.getUserMedia({audio:true}) → 请求麦克风权限
  ↓
MediaRecorder.start() → 开始录音，按钮变红 "⏹ 停止"
  ↓
用户点击"⏹ 停止"
  ↓
MediaRecorder.stop()
  ↓
onstop 回调 → blob → FormData → POST /api/record-audio/{id}
  ↓
后端 audio.py:
  1. 保存临时文件
  2. pydub.AudioSegment.from_file() → 读取
  3. .export(format="mp3") → 转换为 rec_{id}.mp3
  4. UPDATE phrases SET audio_file = 'rec_{id}.mp3'
  5. 删除临时文件
  ↓
返回 {"filename":"rec_5.mp3"}
  ↓
前端显示"录制成功 ✓"
```

---

## 六、关键知识点总结

### 6.1 Python 后端

| 概念 | 说明 | 代码示例 |
|------|------|----------|
| FastAPI | 高性能 Web 框架 | `@router.get("/path")` |
| APIRouter | 模块化路由 | `router = APIRouter()` |
| Depends | 依赖注入 | `conn=Depends(get_connection)` |
| Pydantic | 数据校验 | `class X(BaseModel):` |
| SQLite | 文件数据库 | `sqlite3.connect("file.db")` |
| PRAGMA | SQLite 配置 | `PRAGMA foreign_keys=ON` |
| UPSERT | 插入或更新 | `INSERT ... ON CONFLICT DO UPDATE` |
| COALESCE | NULL 替代 | `COALESCE(col, 默认值)` |

### 6.2 JavaScript 前端

| 概念 | 说明 | 代码示例 |
|------|------|----------|
| fetch | HTTP 请求 | `fetch('/api/xxx')` |
| async/await | 异步操作 | `const data = await fetch(url)` |
| MediaRecorder | 浏览器录音 | `new MediaRecorder(stream)` |
| getUserMedia | 获取麦克风 | `navigator.mediaDevices.getUserMedia({audio:true})` |
| SpeechSynthesis | 语音合成 | `new SpeechSynthesisUtterance(text)` |
| innerHTML | 设置 HTML | `el.innerHTML = '<div>...</div>'` |
| classList | CSS 类操作 | `el.classList.toggle('active')` |
| dataset | data-* 属性 | `el.dataset.id = 123` |

### 6.3 CSS

| 概念 | 说明 | 代码示例 |
|------|------|----------|
| flexbox | 弹性布局 | `display: flex; flex: 1` |
| 3D 翻转 | CSS transform | `transform: rotateY(180deg)` |
| perspective | 3D 透视 | `perspective: 1200px` |
| backface-visibility | 背面隐藏 | `backface-visibility: hidden` |
| @keyframes | 动画关键帧 | `@keyframes pulse { ... }` |
| linear-gradient | 渐变色 | `background: linear-gradient(...)` |

---

## 七、如何手写复现

### 步骤 1：搭建项目结构

```bash
mkdir english-learning-system
cd english-learning-system
mkdir -p app/routers public/src data
touch app/__init__.py app/routers/__init__.py
```

### 步骤 2：先写 requirements.txt

```
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart>=0.0.20
pydub>=0.25.1
```

### 步骤 3：实现数据库层 (database.py)

定义四个表：categories → phrases → progress → stats。注意外键和级联删除。

### 步骤 4：实现数据模型 (models.py)

定义 Pyandtic 模型：CategoryOut, PhraseOut, PhraseCreate, PhraseUpdate, ProgressUpdate, StatsOut。

### 步骤 5：实现路由

按顺序：categories → phrases → progress → stats → audio。每个文件创建 APIRouter，注册 CRUD 接口。

### 步骤 6：组装入口 (main.py)

创建 FastAPI 实例 → 添加 CORS → include_router → 挂载静态文件。

### 步骤 7：写前端 HTML

先写静态结构：卡片（正反面）、测试区域、侧边栏、模态框。所有动态内容用 id 标记。

### 步骤 8：写前端 CSS

从整体布局开始 → 卡片 → 测试 → 侧边栏 → 模态框。用 flexbox 布局，先做桌面版再加响应式。

### 步骤 9：写前端 JS

分为两大块：
1. app.js：学习页面的 API 对象 → init → 分类 → 短语 → 卡片渲染 → 事件绑定 → 测试
2. admin.js：后台的 CRUD → 分类管理 → 短语管理 → 表单 → 音频管理 → 录音

### 步骤 10：准备种子数据

创建 data/phrases.json，格式为 `[{"category":"分类名","phrases":[{"en":"...","zh":"..."}]}]`。

### 步骤 11：测试运行

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
# 浏览器打开 http://localhost:8000
```

---

## 八、常见问题 FAQ

**Q: 为什么用 SQLite 不用 MySQL？**
A: SQLite 是文件数据库，无需安装配置，适合单用户/小项目。

**Q: 录音功能需要 HTTPS 吗？**
A: `getUserMedia` 在非 localhost 环境下需要 HTTPS，部署到服务器时请配置 SSL。

**Q: 语音合成发音不准确怎么办？**
A: 建议为短语录制真人 MP3，系统会优先播放音频文件。

**Q: 如何备份数据？**
A: 直接复制 `data/english.db` 文件即可。SQLite 数据库是单文件。

**Q: 正确率算法是怎样的？**
A: 连续正确 3 次 = 已掌握，中间错一次就重新计数。
